version: 0.2

phases:
  install:
    on-failure: ABORT
    runtime-versions:
      nodejs: 20.x
    commands:
      - corepack enable pnpm
      - pnpm install --frozen-lockfile

  build:
    on-failure: ABORT
    commands:
      - pnpm build
